# set is a collection of well defined objects.

# *****************************properties of sets ******************************

# set is a collection of unique elements.no duplicate elements are allowed.
# set is unordered  ->> Elements order doesn't matter.
# unindexed -->> we cannot access elements using index.
# set is mutable.
# There is no way to change items in set, but we can add or remove items.
#disctionary and set both are defined using curly braces {}. so we can use  set() function to create a empty set.


empty_set = set() # empty set
empty_dictionary = {} # empty dictionary



s={10,220,3,4,0,50,5,"Anand"} # duplicate elements are not allowed
print(s) # if we try to add duplicate elements, it will ignore the duplicates.
 
